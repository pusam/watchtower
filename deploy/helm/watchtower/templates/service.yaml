apiVersion: v1
kind: Service
metadata:
  name: {{ include "watchtower.fullname" . }}
  labels:
    {{- include "watchtower.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "watchtower.selectorLabels" . | nindent 4 }}
